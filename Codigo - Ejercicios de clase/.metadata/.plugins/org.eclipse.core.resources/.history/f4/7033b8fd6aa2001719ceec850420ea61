package ejercicio;

import java.util.Arrays;


public class Ejercicio {

	public static void main(String[] args) {
		SopaDeLetra sopita = new SopaDeLetra("entrada.in");
		/*
		for (int i = 0; i < sopita.getSopaDeLetras().length; i++) {					
			System.out.println(Arrays.toString(sopita.getSopaDeLetras()[i]));			
		}
		*/
		long startTime, endTime, duration = 0, cantidad=100;
		
		for(int i = 0; i< cantidad; i++)
		{
			startTime = System.nanoTime()/1000;
			sopita.prepararResultado();
			endTime = System.nanoTime()/1000;
			duration += (endTime - startTime);
		}
		System.out.println("prepararResultado: 			" + (duration/cantidad)*1000 + " nanosegundos");

		duration = 0;
		for(int i = 0; i< cantidad; i++)
		{
			startTime = System.nanoTime()/1000;
			sopita.prepararResultadoOptimizado();
			endTime = System.nanoTime()/1000;
			duration += (endTime - startTime);
		}
		System.out.println("prepararResultadoOptimizado: 		" + (duration/cantidad)*1000 + " nanosegundos");

	}

}
